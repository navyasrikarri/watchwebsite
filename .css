/* styles.css */

/* Header styles */
/* CSS for images in the watches section */

/* Watch item styles */
.watch-item {
    width: 48%;
    /* Adjust the width to fit two watches side by side */
    border: 1px solid #ccc;
    padding: 20px;
    margin: 5px;
    background-color: #f9f9f9;
    /* Light gray background for watch items */
    display: inline-block;
    /* Use inline-block to display items side by side */
}

/* Allow the height to adjust while maintaining the aspect ratio */
.watch-image {
    width: 100%;
    background-size: cover;
    height: 170px;
}


header {
    background-color: #333;
    color: #fff;
    text-align: center;
    padding: 20px;
}

/* Background color for the body */
body {
    background-color: #f4f4f4;
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

/* Watch section styles */
.watches {
    display: flex;
    justify-content: space-between;
    padding: 20px;
    background-color: #fff;
    /* Set a white background for the watches section */
}

/* Watch item styles */

/* Sidebar styles */
.sidebar {
    width: 30%;
    /* Set the width for the sidebar */
    padding: 20px;
    margin: 5px;
    background-color: #333;
    /* Dark background color for the sidebar */
    color: #fff;
}

/* Links in the sidebar */
.sidebar a {
    color: #fff;
    text-decoration: none;
    display: block;
    margin-bottom: 10px;
}

/* Modal styles */
.modal {
    display: none;
    position: fixed;
    z-index: 1;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);
    /* Semi-transparent black background for the modal */
}

/* Add more CSS styles for other sections as needed */

/* Footer styles */
footer {
    background-color: #333;
    color: #fff;
    text-align: center;
    padding: 10px;
}